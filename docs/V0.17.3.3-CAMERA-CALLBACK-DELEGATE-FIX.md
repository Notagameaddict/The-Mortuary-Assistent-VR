# v0.17.3.3 — Camera callback delegate fix

Visual Studio reported:

```text
CS0428: Cannot convert method group 'OnCameraPreRender'
to non-delegate type 'Camera.CameraCallback'
```

In this IL2CPP Unity reference, the callback must be constructed explicitly:

```csharp
new Camera.CameraCallback(OnCameraPreRender)
```

## Replace

- `PluginInfo.cs`
- `Core/RuntimeBehaviour.cs`

No native DLL rebuild is required.

## Expected build result

```text
1 succeeded, 0 failed
```
