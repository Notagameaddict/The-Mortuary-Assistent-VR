# v0.31.12 build fix

v0.31.11 contained a malformed edit in EndFrameWithProjectionAndToolUiLayer.
That caused cascading C# parser/scope errors such as uiImageSet/quadLayerPointer
not existing and missing `;` / `}`.

v0.31.12 restores the last known compiling v0.31.10 backend implementation
and bumps the plugin version. This is intentionally a build-recovery release.

Replace:
- PluginInfo.cs
- XR/OpenXrNativeBackend.cs

No native probe rebuild is required.
